name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  checks:
    uses: ./.github/workflows/reusable-checks.yml
    secrets: inherit
    with:
      python-version: "3.14"
      # No JS remains in this repo since the example host dissolved (P7); the
      # composed-web coverage lives in the stack lane.
      pnpm-enabled: false
      stack-enabled: true
