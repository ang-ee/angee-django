---
"@angee/ui": patch
"@angee/refine": patch
---

Preserve edits made during form saves using React Hook Form 7.81's native defaults API. Keep dirty line arrays intact across refreshes and report observed server conflicts before full-list writes. Preserve explicit URL clears and custom route defaults.

Let authored infinite consumers own continuation through server cursors, and preserve server order when refreshed heads displace loaded history.
