---
"@angee/ui": patch
"@angee/metadata": patch
---

Honor custom relation widgets in forms and read object-valued many-to-many line relations.
Keep full-form removable chips; use compact multiple selection only in editable cells.
Forms without an update operation or custom submit are read-only for existing records.
Saving a dirty but semantically unchanged draft resets it to server values without a mutation.
