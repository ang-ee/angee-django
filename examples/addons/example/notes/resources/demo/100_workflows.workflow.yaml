_meta:
  model: workflows.Workflow
rows:
  - xref: note_publish_approval
    fields:
      name: Note publish approval
      description: Demo workflow for reviewing a note before publish.
      subject_declaration: notes.note
      max_steps: 20
      budget: {}
