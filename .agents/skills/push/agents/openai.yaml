interface:
  display_name: "Push"
  short_description: "Commit and publish an Angee workspace branch."
  default_prompt: "Use $push to commit reviewed changes and publish the current workspace branch."
policy:
  allow_implicit_invocation: false
