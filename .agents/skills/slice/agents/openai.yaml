interface:
  display_name: "Slice"
  short_description: "Diagnose, review, and verify one requested change."
  default_prompt: "Use $slice to implement, review, and verify this change within the authorized scope."
policy:
  allow_implicit_invocation: false
