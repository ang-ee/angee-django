interface:
  display_name: "Pull"
  short_description: "Merge changes into the current Angee workspace."
  default_prompt: "Use $pull to bring changes into the current Angee workspace branch."
policy:
  allow_implicit_invocation: false
