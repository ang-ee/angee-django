# GLM-OCR accepts page images and therefore must remain a recognition-capable
# catalogue model even when the generic Ollama model listing reports chat only.
- xref: model_glm_ocr
  provider: agents_integrate_ollama.provider_ollama_demo
  name: glm-ocr:latest
  display_name: GLM-OCR
  model_use: multimodal
