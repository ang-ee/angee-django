[addon]
name = "angee.workflows_extraction_glm"
description = "Local GLM-OCR extraction through Ollama's native API"
keywords = ["ocr", "glm", "ollama", "local"]
category = "Integration"
depends_on = ["angee.workflows_extraction", "angee.agents_integrate_ollama"]

[resources]
install = [
  { path = "resources/install/040_agents.inferencemodel.yaml", adopt = ["provider", "name"] },
]
