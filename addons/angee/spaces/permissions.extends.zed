// @rebac_package: spaces
// @rebac_schema_revision: 5

definition messaging/thread {
    relation group: spaces/group
    relation selected_group: spaces/group // rebac:field=groups

    permission read = group->read + selected_group->read
    permission write = group->post + selected_group->post
}
