# Angee addon manifest. The declarative contract hatch-angee compiles into
# the package's [project] + entry points; see hatch-angee's native AddonManifest.
[addon]
name = "angee.agents_runtime_pydantic"
description = "In-process pydantic-ai runtime for durable agent sessions"
keywords = ["agents", "pydantic-ai", "runtime"]
category = "Agents"
depends_on = ["angee.agents", "angee.integrate", "angee.mcp"]
dependencies = [
  "pydantic-ai-slim[mcp]>=2.13",
]
