# Angee addon manifest. The declarative contract hatch-angee compiles into
# the package's [project] + entry points; see angee/addons.py (AddonContract).
[addon]
name = "angee.platform_integrate_operator"
description = "The operator transport for addon install/uninstall — edits settings.yaml + rebuilds through the operator file API."
keywords = ["marketplace", "addons", "operator"]
category = "Integration"
depends_on = ["angee.platform", "angee.operator"]
