# Angee addon manifest. The declarative contract hatch-angee compiles into
# the package's [project] + entry points; see angee/addons.py (AddonContract).
[addon]
name = "angee.platform_integrate_vcs"
description = "The VCS/remote tier of the addon marketplace — addons known from a repo, not yet installed."
keywords = ["marketplace", "addons", "vcs"]
category = "Integration"
depends_on = ["angee.platform", "angee.integrate"]
