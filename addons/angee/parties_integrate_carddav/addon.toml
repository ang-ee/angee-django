# Angee addon manifest. The declarative contract hatch-angee compiles into
# the package's [project] + entry points; see hatch-angee's native AddonManifest.
[addon]
name = "angee.parties_integrate_carddav"
description = "Bidirectional CardDAV contacts with sync-token deltas and conditional vCard writes"
keywords = ["carddav", "contacts", "sync"]
category = "Integration"
depends_on = ["angee.parties", "angee.integrate"]
dependencies = [
  "defusedxml>=0.7,<1",
  "vobject>=0.9.9",
]
