# Angee addon manifest. The declarative contract hatch-angee compiles into
# the package's [project] + entry points; see hatch-angee's native AddonManifest.
[addon]
name = "angee.workflows_integrate"
description = "Workflow admission and archive extraction for integration-owned ingest surfaces"
keywords = ["workflows", "integrate", "archives"]
category = "Workflows"
depends_on = ["angee.workflows", "angee.storage", "angee.integrate"]
