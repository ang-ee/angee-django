# Angee addon manifest. The declarative contract hatch-angee compiles into
# the package's [project] + entry points; see angee/addons.py (AddonContract).
[addon]
name = "angee.workflows_integrate"
description = "Archive extraction workflow steps for integration-owned ingest surfaces"
keywords = ["workflows", "integrate", "archives"]
category = "Workflows"
depends_on = ["angee.workflows", "angee.integrate", "angee.storage"]
