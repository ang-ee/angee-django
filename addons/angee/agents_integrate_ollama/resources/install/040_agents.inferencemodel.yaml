# The agents inference owner serves this GLM-OCR deployment. Extraction's plain
# mapping and recognition functions call it through the workflow inference helper.
- xref: model_glm_ocr
  provider: agents_integrate_ollama.provider_ollama_demo
  name: glm-ocr:latest
  display_name: GLM-OCR
  model_use: multimodal
